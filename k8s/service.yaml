apiVersion: v1
kind: Service
metadata:
  name: sentinel-soc-agent-service
  namespace: sentinel-soc
  labels:
    app: sentinel-soc-agent
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: sentinel-soc-agent
