apiVersion: v1
kind: Service
metadata:
  name: sentinel-soc-service
  namespace: sentinel-soc
  labels:
    app: sentinel-soc-agent
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: sentinel-soc-agent
